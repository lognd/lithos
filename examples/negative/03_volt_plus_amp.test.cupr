# WO-83 slice B corpus member (charter toolchain/37 sec. 3): the
# diagnostic-expectation negative-twin form, exercised directly
# against the real negative fixture `03_volt_plus_amp.cupr` (its
# `.test.cupr` sibling per the discovery convention) -- no scenario
# override needed, the design is already deliberately broken.

test volt_plus_amp_case:
    expect:
        diagnostic E0101 on bad
