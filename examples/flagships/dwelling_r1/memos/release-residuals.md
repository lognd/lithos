# dwelling_r1 -- release residuals memo

Engineering memo (D207) backing this project's `waive ... by
doc(memos/release-residuals.md)` deviations: per accepted waiver, why
the residual is genuinely unbounded (the wall), and the discharge path
that would retire it. Hash-pinned like any catalog doc; unsigned, so
it confers `community` tier (INV-14).

## Accepted -- bare-import conformance edge (D195.3/D213)

`panel.cupr`'s `import(dwelling.calx)` claim carries no scalar text
window to check (a bare module import statement has no comparable
value), the same landed conformance edge `substation.cupr` and
`program.calx` (timber_pavilion) already waive under this exact basis.
Addressable per D213; verdicts untouched (INV-2).

## Named refusal -- panel/breaker catalog content (D250 sec. 3)

`MainPanelBox`'s bus ampacity, main-breaker/lugs rating, and branch-
slot count are NOT declared here as catalog facts (no breaker/panel
manufacturer catalog record exists in `std.power` beyond WO-134/134B's
landed transformer catalogue) -- the working-clearance siting claim
this project DOES discharge (`panel.cupr`'s `front:` obligation) does
not require that catalog content; a panel-bus-ampacity claim would,
and is therefore not attempted here (see `docs/workflow/work-orders/
WO-167-dwelling-wiring-program.md` close-out for the named follow-up).
