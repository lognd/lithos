/* generated by regolith ship --emit-profile debug (WO-125, charter 40 sec. 1) */
#ifndef REGOLITH_DEBUG_TAPS_H
#define REGOLITH_DEBUG_TAPS_H

/* Trace hooks compile to nothing unless REGOLITH_DEBUG_TAPS
   is defined by the debug build. */
#ifdef REGOLITH_DEBUG_TAPS

typedef struct {
    int channel;
    const char *kind;
    const char *target;
    const char *why;
} regolith_tap_t;

static const regolith_tap_t REGOLITH_TAP_TABLE[] = {
    /* REGOLITH-TAP ch=0 target=LaJigSi.usb_dp_dm */
    {0, "bus", "LaJigSi.usb_dp_dm", "bring-up: 'is the USB pair even toggling?' is the first question when the jig will not enumerate (claim usb_diff_z0 names this net)"},
};

#define REGOLITH_TAP_COUNT 1
void regolith_trace_hook(int channel, unsigned long value);
#define REGOLITH_TRACE(ch, value) regolith_trace_hook((ch), (value))

#else /* !REGOLITH_DEBUG_TAPS: release -- compiles to nothing */

#define REGOLITH_TAP_COUNT 0
#define REGOLITH_TRACE(ch, value) ((void)0)

#endif /* REGOLITH_DEBUG_TAPS */
#endif /* REGOLITH_DEBUG_TAPS_H */
