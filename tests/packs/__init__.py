"""The pack-protocol conformance suite (WO-20, design doc D-F).

The regolith repo keeps the CONTRACT external model packs are built
against: :mod:`tests.packs.conformance` is the reusable suite a pack
runs against itself, and the fixture pack/solver here exercise it
in-repo so the protocol cannot drift unnoticed.
"""
