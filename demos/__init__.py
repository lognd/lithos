"""WO-108 optimization proof packs.

One runnable script per optimizer surface (`demos/demo*_*.py`). Each runs
the REAL regolith pipeline end to end and emits PHYSICAL artifacts
(drawings SVG/PDF, STEP, GLB + offline viewer, BOM, lockfile) under
`demos/out/<demo>/`, with a content-hashed `manifest.json` and a
narrating `PROOF.md`. See `docs/guide/proving-your-optimizations.md`.
"""
