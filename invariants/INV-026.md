---
id: INV-026
statement: "Every default behavior in either language is conservative, local in effect, and lockfile-materialized (meta-invariant over the spec itself)."
criticality: medium
evidence:
  - tests/invariants/test_inv_26_defaults_test_compliance_meta_invariant.py::test_inv_26_free_variable_resolution_infeasible_is_loud
  - tests/invariants/test_inv_26_defaults_test_compliance_meta_invariant.py::test_inv_26_derived_workload_wrong_default_is_loud
  - tests/invariants/test_inv_26_defaults_test_compliance_meta_invariant.py::test_inv_26_implicit_by_spec_contradiction_is_loud
---
Checked mirror of INV-26 (Defaults-test compliance,
meta-invariant) in `docs/spec/regolith/13-invariants.md`; that file is
the normative source.

Mechanism: this is an invariant *over the spec itself* -- the defaults
are enumerable (free-variable resolution, implicit `by spec`, local
tolerance allocation, canonical `any`, eager candidate acceptance,
derived workloads). `WorkloadRealizationModel` is the concrete
discharge model for the derived-workload default (EOPEN-15 rule 3):
declared edges defer honestly rather than silently passing.

Argument summary: enforced editorially at spec time and executably at
test time -- each default gets a spurious-failure-not-silent-pass
test; this file anchors one representative default (the rest are
enumerated in the WO-17 test family, not individually anchored here).

Anchor (primary enforcing site): `python/regolith/harness/models/workload_realization.py :: WorkloadRealizationModel`
