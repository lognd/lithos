---
id: INV-027
statement: Build results are invariant to source file layout; identity keys on declared names, never file paths
criticality: medium
evidence:
  - tests/invariants/test_inv_27_file_layout_invariance.py::test_inv_27_split_across_files_preserves_identities
---

Checked mirror of docs/spec/regolith/13-invariants.md INV-27 (the
normative source). REPRESENTATIVE ANCHOR (T-0033 coordinator ruling):
this is an absence proof -- no code path anywhere keys on file
identity -- held by the whole resolver's name-keyed construction.
INV002 requires a code anchor, so the anchor sits on the
REPRESENTATIVE site where identity is defined name-keyed
(`crates/regolith-sem/src/entity.rs::EntityId`), explicitly a
representative of the whole-construction property, not a unique
enforcing chokepoint; the layout-shuffle evidence test is the real
guard and fails loudly if any future path sneaks file identity into
an identity key. Reopen trigger: any resolver change that introduces a
path-derived key must revisit this posture.
