---
id: INV-020
statement: "Nothing expensive runs until everything cheaper has passed for the affected subjects; the pipeline gates L(n) on L(<n) verdicts per subject."
criticality: high
evidence:
  - tests/invariants/test_inv_20_check_gating.py::test_inv_20_poisoned_subject_is_gated_but_clean_sibling_is_not
---
Checked mirror of INV-20 (Check gating) in
`docs/spec/regolith/13-invariants.md`; that file is the normative
source.

Mechanism: `run_checks` is the single invocation path that gates each
static tier per subject; a poisoned subject (parse error) is excluded
from later passes, while its clean siblings continue.

Argument summary: single invocation path + per-subject gating; there
is no user-reachable "just simulate it" entry that skips the static
tiers.

Anchor (primary enforcing site): `crates/regolith-lower/src/checks.rs :: run_checks`
