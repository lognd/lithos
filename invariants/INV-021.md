---
id: INV-021
statement: "Every number the designer did not write literally appears in the lockfile with its resolving Cause; causeless values are unrepresentable."
criticality: high
evidence:
  - tests/invariants/test_inv_21_resolution_provenance.py::test_inv_21_every_resolution_carries_a_valid_cause
---
Checked mirror of INV-21 (Resolution provenance) in
`docs/spec/regolith/13-invariants.md`; that file is the normative
source.

Mechanism: the `Cause` enum (dfm/drc, obligation, budget, topology,
planner, extern, derived-intent, policy annotation) is mandatory on
every resolved value; the resolver API cannot construct a resolved
value without one.

Argument summary: by construction of the resolution type -- causeless
values are unrepresentable.

Anchor (primary enforcing site): `crates/regolith-qty/src/resolution.rs :: Cause`
