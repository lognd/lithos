---
id: INV-010
statement: "Given (source, lockfile, tool versions), all decisions and evidence identities are bit-reproducible; numerical evidence is reproducible per each model's `deterministic:` flag."
criticality: high
evidence:
  - tests/invariants/test_inv_10_reproducibility.py::test_inv_10_double_build_is_byte_identical
  - tests/invariants/test_inv_10_reproducibility.py::test_inv_10_obligation_keys_are_stable_across_builds
---
Checked mirror of INV-10 (Reproducibility) in
`docs/spec/regolith/13-invariants.md`; that file is the normative
source.

Mechanism: `discharge_static` keys the evidence cache on the
obligation's content hash folded with the harness registry version; a
model registry bump re-keys and forces recomputation rather than
silently reusing stale evidence.

Argument summary: every nondeterminism source is either pinned
(decisions) or declared and hashed (numerics), so identity never lies
even where values wobble.

Anchor (primary enforcing site): `crates/regolith-lower/src/discharge.rs :: discharge_static`
