---
id: INV-007
statement: "Evidence transfers into any context whose boundary is contained in the proven one; a widened enclosing envelope fails at L2."
criticality: high
evidence:
  - tests/invariants/test_inv_07_boundary_subsumption.py::test_inv_07_enclosing_envelope_wider_than_child_fails
  - tests/invariants/test_inv_07_boundary_subsumption.py::test_inv_07_enclosing_envelope_within_child_is_clean
---
Checked mirror of INV-7 (Boundary subsumption) in
`docs/spec/regolith/13-invariants.md`; that file is the normative
source.

Mechanism: `check_boundary_subsumption` requires, for every shared
boundary quantity, that the enclosing node's envelope is CONTAINED in
the imported/child artifact's proven envelope.

Argument summary: a claim discharged for all environments in E holds
for all environments in any subset of E -- monotone by construction;
the envelope-phrasing rule is what makes one containment direction
uniformly safe.

Anchor (primary enforcing site): `crates/regolith-ir/src/system.rs :: check_boundary_subsumption`
