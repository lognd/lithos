---
id: INV-022
statement: "All foreign content is hash-pinned; drift is an error, never a silent rebuild input."
criticality: high
evidence:
  - tests/invariants/test_inv_22_foreign_content_pinning.py::test_inv_22_tampered_content_under_pin_is_drift
  - tests/invariants/test_inv_22_foreign_content_pinning.py::test_inv_22_vendored_file_reverified_on_read
---
Checked mirror of INV-22 (Foreign-content pinning) in
`docs/spec/regolith/13-invariants.md`; that file is the normative
source.

Mechanism: content addressing at every entry point (import stages,
`extern(ref)`, record revisions, `formats` packages, `toolchain:`
pins); `hash_pcb_file` is one concrete instance -- content-addressing a
`.kicad_pcb` file for pin comparison.

Argument summary: the entry-point list is closed (INV-1's key
components plus L0 imports); a changed file fails the pin comparison
before anything consumes it.

Anchor (primary enforcing site): `python/regolith/realizer/elec/kicad.py :: hash_pcb_file`
