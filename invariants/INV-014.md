---
id: INV-014
statement: "Every evidence item carries a trust tier, and trust floors compare totally; hosting confers no trust."
criticality: high
evidence:
  - tests/invariants/test_inv_14_trust_totality.py::test_inv_14_tiers_are_a_total_order
  - tests/invariants/test_inv_14_trust_totality.py::test_inv_14_hosting_confers_no_trust
  - tests/invariants/test_inv_14_trust_totality.py::test_inv_14_below_floor_stays_below_floor
---
Checked mirror of INV-14 (Trust totality) in
`docs/spec/regolith/13-invariants.md`; that file is the normative
source.

Mechanism: `verify_trust` is the mandatory entry point at every
evidence surface (records, overrides, deviations); tiers form a total
order (certified > tested > community).

Argument summary: by construction of the entry points -- F54 closed
the deviation gap that would otherwise let an evidence item skip
tiering.

Anchor (primary enforcing site): `python/regolith/magnetite/trust.py :: verify_trust`
