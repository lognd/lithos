---
id: INV-002
statement: "No override mechanism converts `violated` into `discharged`; acceptance records reference evidence without altering status."
criticality: high
evidence:
  - tests/invariants/test_inv_02_ladder_safety.py::test_inv_02_a_waiver_never_alters_the_obligation_set
  - tests/invariants/test_inv_02_ladder_safety.py::test_inv_02_an_unjustified_waiver_is_an_overreach_diagnostic
---
Checked mirror of INV-2 (Ladder safety) in
`docs/spec/regolith/13-invariants.md`; that file is the normative
source.

Mechanism: rungs 1/2/4/5 act upstream of obligation identity (INV-1);
rung 3 is verdict-invariant (INV-3); rungs 6/7 attach acceptance
records that reference the evidence hash and never modify status.
`lower_one_waiver` validates the mandatory `basis:` (an unjustified
concession is an overreach diagnostic) and classifies the waiver
against the obligation set without ever mutating `obligations`.

Argument summary: by case analysis over the closed list of rungs (the
list is closed because `12-overrides-and-hints.md` is the single home
for redirect mechanisms).

Anchor (primary enforcing site): `crates/regolith-lower/src/waivers.rs :: lower_one_waiver`
