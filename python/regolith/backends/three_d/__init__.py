"""The 3D artifact family (WO-100 deliverable 3/4; charter 38 sec. 1
decision 6): a deterministic binary glTF (GLB) plus a self-contained,
zero-external-request HTML viewer, per `RealizedGeometry` part and per
`RealizedAssembly`. Renderers of realized IRs, registered like any
renderer through the WO-99 registry seam.
"""
