# Example fluid circuit -- replace with your own design.
# The leading `#` comment block above a declaration is its doc text
# (`regolith doc` renders it; no new syntax).
#
# HONEST ENGINE GAP (WO-53 template audit; narrowed by WO-92): a
# `.fluo` claim like the one below now LOWERS to a scalar discharge
# request (WO-92 recovered the comparator structurally from the
# `fluids.dp(...) <= ...` predicate shape), but no `fluids.*` harness
# model is registered yet, so it discharges as an honest
# `harness.no_model` indeterminate rather than a verdict. The flownet
# itself separately emits an unresolved `conforms` obligation for its
# module import (nothing scalar to compare on either side -- D195).
# `regolith check` passes (no static diagnostic); `regolith build
# --release` and `regolith ship` cannot succeed for any `.fluo`
# source in this repo until a fluid model pack lands. No claim
# substitution in this file can route around that, so none is
# attempted here -- see the WO-53 template audit report.
import std.fluorite (Pipe, Regulator)

medium Water: liquid
    props: registry(water_nist)

flownet Loop(medium=Water):
    reference: ambient(101kPa, 293K)
    nodes: src_out, sink_in
    edges:
        line: Pipe(from=run.main)               (src_out -> sink_in)

require Supply:
    # An honest example claim: the loop pressure drop stays bounded.
    # (Currently indeterminate/deferred -- see the file-level comment.)
    margin: fluids.dp(src_out -> sink_in) <= 50kPa
