# A regolith project manifest (regolith/11 sec. 9): this manifest +
# the source tree + one lockfile. [package] carries only a name until
# published.

[package]
name = "__PROJECT__"

[depends]
"std.quantities" = "^1"
"std.materials" = "^1"
"std.mech" = "^1"
