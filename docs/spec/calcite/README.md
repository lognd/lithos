# calcite -- the civil/architectural track (`.calx`)

Chartered cycle 26 (D133), elaborated cycle 27 (D139): spaces +
structure + envelope over the shared regolith; loads and
building-code compliance as pack content; structural evidence
through the same obligation pipeline (feldspar is the frame/FEA
home). The name's earlier life as the fluid track's draft (`.calc`,
dead since D93) is unrelated; the civil track uses `.calx`.

Reading order:

- [01-charter.md](01-charter.md) -- the SETTLED v1 charter: scope, the three
  artifact families, lowering shape, non-goals with reopen criteria,
  corpus plan. Still the normative root; 02-04 elaborate it.
- [02-language.md](02-language.md) -- sites/grids, spaces, circulation nets, members,
  transfers, structures (the load-path net), loads, envelope
  assemblies, claims, cross-track composition, vocabulary.
- [03-lowering.md](03-lowering.md) -- the L0-L6 table, elaboration rules, the two
  net disciplines (E0204-E0209), the `frame` payload field list,
  obligation shapes, the ship surface.
- [04-open-questions.md](04-open-questions.md) -- deferrals with reopen criteria ONLY (the
  COPEN queue is empty by construction).

Corpus: `examples/tracks/calcite/` (bus_shelter, pole_barn,
footbridge, retaining_wall) + `examples/flagships/small_office/` (the
cross-track flagship: `.calx` + `.fluo` + `.cupr`).

Status: elaboration complete, awaiting owner ratification (WO-46
flips on ratification); front end = WO-47, lowering + `std.civil`
packs = WO-48, drawing sheets = WO-50 (D140).
