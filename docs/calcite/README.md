# calcite -- the civil/architectural track (`.calx`)

Chartered cycle 26 (D133): spaces + structure + envelope over the
shared regolith; loads and building-code compliance as pack content;
structural evidence through the same obligation pipeline (feldspar
is the frame/FEA home). The name's earlier life as the fluid track's
draft (`.calc`, dead since D93) is unrelated; the civil track uses
`.calx`.

- `01-charter.md` -- the SETTLED v1 charter: scope, the three
  artifact families, lowering shape, non-goals with reopen criteria,
  corpus plan. Read this first; it is the normative source until
  WO-46 elaborates the full track docs (02-language, 03-lowering,
  04-open-questions) in the fluorite pattern.

Status: charter settled; spec elaboration = WO-46, front end =
WO-47, lowering + `std.civil` packs = WO-48.
