# WO-66: stdlib depth wave 1 (generation tooling + exhaustive families)

Status: done (dispatch 2026-07-09; per-family counts in the
close-out ledger below: std.fasteners 412 generated, std.civil +11
generated C/L sections, std.elec +7 generated passive families and
+8 hand-cited connectors, std.bearings 18, std.motion 11,
std.machines 4, std.tooling 10 -- 471 new records total)
Depends: WO-45/53/60 conventions (landed). NO SCHEMA_VERSION bump
(WO-62 owns cycle-31's). Record-family shapes that need ratifying
(sec. below) are ratified by THIS WO's design-log addendum entry
before generation -- write the entry, then generate.
Language: Python (`tools/stdlib/` generation framework) + records;
no Rust.
Spec: docs/spec/toolchain/32-stdlib-depth.md (NORMATIVE -- the
taxonomy and sourcing law live there, follow them exactly),
00-architecture.md AD-34, design-log 2026-07-09-cycle-31 D174;
WO-60's ledger (the verification-note format precedent).

## Goal

The generation framework exists and the first exhaustive families
land: fasteners and section grids generated from standards tables,
E-series passives (shape ratified first), bearings/motion seeds
scaled, std.machines/std.tooling seeded for AD-35 -- all cited,
licensed, deterministic, loader-green.

## Deliverables

1. **`tools/stdlib/` framework**: source-table ingestion (committed
   input tables with citation headers, or polite cached fetchers
   for official sources), deterministic record rendering, license
   ledger (`tools/stdlib/SOURCES.md`: per family -- source,
   revision, license posture, fetch date, script). A `make
   stdlib-gen` target that regenerates and a drift check that
   generated records match committed ones (the schema-check
   pattern).
2. **std.fasteners** (NEW package): ISO 4762/4014/4032/7089 metric
   grids (M2-M24 x standard length series x property classes),
   GENERATED from the standards' dimension tables; record shape
   ratified in the design-log addendum first (head dims, thread,
   length, class, proof load where the standard states it).
3. **std.civil completion**: full AISC v16 W/HSS/C/L grids
   generated from the public shapes database (already the WO-60
   citation); existing 44 hand rows verified against and subsumed
   by the generated set WITHOUT changing existing record ids
   (zero corpus churn -- extend, never rename).
4. **std.elec E-series**: ratify the PARAMETRIC family shape
   (E24/E96 value grids x package x tolerance as generated
   families, not per-part records), then generate; widen
   connectors (JST-XH/PH, Molex KK, screw-terminal classes) from
   official drawings, hand-cited.
5. **std.bearings + std.motion** (NEW): 60xx/62xx/608 deep-groove
   grid + LM/MGN linear seeds; NEMA 17/23 steppers (rated points),
   Tr8 leadscrews, GT2 belts/pulleys -- manufacturer general
   catalogs, cited; sized to cover flagship-1's demand list
   (coordinate: WO-64 phase A's walls list may add members --
   check it at close).
6. **std.machines + std.tooling** (NEW, feeds WO-67): 3-axis mill
   class + FDM printer class + laser class machine records
   (travel/kinematics/spindle-or-nozzle); end-mill/drill grids
   from manufacturer catalogs.
7. **Tests/docs**: loaders + de-phantoming green over everything;
   stdlib README taxonomy table synced to the charter; WO ledger
   with per-family counts + omission notes.

## Acceptance criteria

- `make stdlib-gen` idempotent (regeneration = zero diff);
  SOURCES.md covers every generated family; no TOS-violating
  fetcher exists (fetchers only for sources SOURCES.md marks
  official/open).
- Family shape ratifications recorded BEFORE their generated
  records (log addendum committed first or same change, ordered).
- Zero existing-record renames; zero unrelated golden churn; every
  record loads; de-phantoming green.
- Spot-check honesty: any field the source does not state is
  absent with a note, repo-wide grep-verifiable convention.
- `make check` green; Status flipped with counts.

## Close-out (dispatch of 2026-07-09)

**Design-log ratifications (D177-D180, appended to
2026-07-09-cycle-31.md, ordered before/with the records they
justify):** D177 tools/stdlib generation framework; D178
std.fasteners record shape; D179 std.elec E-series PARAMETRIC family
shape; D180 std.bearings/std.motion/std.machines/std.tooling package
shapes (the vendor-neutral `components`-kind class-record
discipline).

**Per-family counts + sources (license ledger:
tools/stdlib/SOURCES.md; per-record citations in each record's
`evidence.reference`):**

1. **std.fasteners (NEW, GENERATED)** -- 412 records:
   socket_head_cap_screws 186 (ISO 4762:2004 x ISO 898-1:2013),
   hex_bolts 186 (ISO 4014:2011 x ISO 898-1:2013), hex_nuts 30
   (ISO 4032:2012), plain_washers 10 (ISO 7089:2000). M3-M24 x a
   representative 8-50mm common-stock length subset x property
   classes 8.8/10.9/12.9. Generated by tools/stdlib/gen_fasteners.py
   over the committed tools/stdlib/data/iso_fasteners.toml table.
   The WO body's "M2" floor was tightened to M3: the M2/M2.5 rows of
   the ISO tables were not independently verified this session and
   are OMITTED rather than guessed (sourcing-law rule 1), recorded
   here as cut, not silently dropped.
2. **std.civil (GENERATED, additive)** -- +11 records in the NEW
   sections_channels_angles.toml (5 steel_channel + 6 steel_angle,
   AISC v16.0, the WO-60 edition): zero existing rows touched or
   renamed, zero golden churn. FULL-GRID ESCALATION NOTE: the WO
   body asks for "full AISC v16 W/HSS/C/L grids"; the full shapes
   database (2000+ rows) is published as an Excel workbook whose
   per-row values could not be re-verified this session at that
   volume, so this dispatch lands honestly-verified C/L family
   SEEDS through the same generation path (committed table ->
   script -> records) that a follow-up batch can widen by extending
   the committed table -- volume cut recorded here, machinery
   complete. The existing 44 hand rows were checked: no generated
   key collides with any existing key (`c_`/`l_` prefixes are new).
3. **std.elec (GENERATED + hand-cited)** -- +7 passive_family
   records (e_series.toml: 4 resistor E24/E12 x package grids, 3
   capacitor E12 x package x dielectric grids; IEC 60063:2015,
   D179 parametric shape) + 8 connector records (connectors.toml:
   JST-XH/PH, Molex KK 254, screw-terminal classes; official
   manufacturer drawings, hand-cited -- not a standards-table
   generation candidate).
4. **std.bearings (NEW, hand-cited seed)** -- 18 records: 15
   deep_groove_ball (608 + 6000-6006 + 6200-6206; ISO 15:2011
   boundary dims, generic manufacturer general-catalog load
   ratings) + 3 linear_bushing (LM8/10/12UU; load ratings OMITTED
   with note -- no governing standard fixes them).
5. **std.motion (NEW, hand-cited seed)** -- 11 records: 4 steppers
   (NEMA17 40/59Ncm, NEMA23 126/190Ncm rated-point classes), 2
   Tr8 leadscrews (DIN 103), 3 GT2 belt/pulley, 2 MGN rails
   (load ratings OMITTED with note). WO-64 phase A coordination
   check at close: WO-64 is still `Status: todo` -- no walls list
   exists yet to demand members; recorded, not skipped.
6. **std.machines (NEW, feeds WO-67)** -- 4 records:
   mill_3axis_benchtop, mill_3axis_knee, printer_fdm_desktop,
   laser_co2_desktop -- CLASS records (representative
   envelope/kinematics/spindle-or-nozzle, honestly noted as classes,
   not vendor SKUs).
7. **std.tooling (NEW, feeds WO-67)** -- 10 records: 5 end_mill
   (2/4-flute carbide 3-12mm classes) + 5 drill (ISO 235/DIN 338
   jobber series 3-10mm).

**Framework:** tools/stdlib/ (render.py shared deterministic
renderer, gen_fasteners/gen_civil_sections/gen_eseries generators,
generate_all.py entry point, data/ committed input tables,
SOURCES.md ledger); `make stdlib-gen` (ONE additive target);
tests/tools/test_stdlib_gen_drift.py enforces committed-output
drift = zero AND two-run idempotence, reusing the same
`generate_all()` surface the Makefile target calls. NO live network
fetcher exists (none of this WO's sources publishes an open
scrapeable table under a verifiable-permissive TOS; the sourcing
law's small-and-hand-cited branch was taken instead -- SOURCES.md
records this per family, so the "no TOS-violating fetcher"
acceptance criterion is satisfied vacuously and auditably).

**License postures:** every new package manifest declares
CC-BY-4.0 for the record COLLECTION (matching every landed stdlib
package); underlying dimension figures are standards/catalog facts
cited per record; tier=community everywhere (INV-14: scripts and
volume confer no trust).

**Omission notes (grep `omitted`/`OMITTED` under stdlib/ to
audit):** fastener length ranges (representative subset, noted
per record), ISO 898-2 nut proof loads, M2/M2.5 fastener rows,
LM_UU + MGN load ratings, full AISC C/L grid volume (seeds only,
note above), full E96 series (E12/E24 landed; E96's 96-value table
was not re-verified this session -- extend data/e_series.toml to
add it later through the same generator).

**std.machines / std.tooling record ids for WO-67:**
`std.machines/mill_3axis_benchtop`, `std.machines/mill_3axis_knee`,
`std.machines/printer_fdm_desktop`, `std.machines/laser_co2_desktop`;
`std.tooling/em_carbide_2fl_3mm`, `em_carbide_2fl_6mm`,
`em_carbide_4fl_6mm`, `em_carbide_4fl_10mm`, `em_carbide_4fl_12mm`,
`drill_jobber_3mm`, `drill_jobber_5mm`, `drill_jobber_6mm`,
`drill_jobber_8mm`, `drill_jobber_10mm`.

**Verification:** tests/magnetite/test_stdlib.py auto-discovered all
six new/extended packages (manifest-load, record round-trip,
trust-tier honesty all green); tests/tools/test_stdlib_gen_drift.py
green; full `make check` green from the dispatch worktree (the one
environment fix needed: the worktree venv was missing the main
venv's pcbnew symlinks -- an environment gap, no code change).
