# Design log -- cycle 38 (2026-07-18/19)

Context: the frob-conversion campaign (work orders -> tickets.md,
invariants -> invariants/, design/lithos.strata, zero-gate baseline)
landed this cycle; see tickets T-0031/T-0032/T-0034 and memory of
record. This log records the owner-directed design decisions opened
on top of that baseline.

## D267 -- the boundary charter is adopted (AD-43..AD-47)

`docs/spec/toolchain/44-boundary-charter.md` is normative: the
six-layer model (AD-43), the `regolith.surface` UI read facade
(AD-44), artifact provenance tiers in the index row (AD-45),
registration-derived classification (AD-46), and the realizer
capability registry (AD-47). Grounded in the 2026-07-19 as-built
recon (ambiguity census A1..A8; dossier in the session scratchpad,
condensed into the charter's own text).

Known alignment debts the charter supersedes (fix tickets, not
silent edits):

- D259's "exactly two contacts" statement for graphite is amended to
  three (the surface facade import). The graphite imports of
  `regolith.orchestrator.*` found 2026-07-19 in
  `graphite/service/reports.py` and `graphite/server/routes/build.py`
  are VIOLATIONS under both old and new law until the facade lands
  and graphite migrates onto it; remediation is part of the AD-44
  ticket, enforced thereafter by frob forbidden-import policy in
  graphite and a strata flow claim in lithos.
- AD-22's promotion rule now requires an open promotion ticket bound
  to every forward-authored contract type (`FeatureProgram` is the
  standing instance).

Fact-check results folded in: WO-150/DimensionedValue is landed and
binding (INV-34); feldspar is on the unified `regolith.plugins`
entry-point group (AD-26 follow-up already done).

## D268 -- the capability extension program

Owner directive (2026-07-19): extend generation capabilities so the
toolchain can, in an automated pass, produce (1) hardened tool-steel
parts profiled on wire EDM, bolted to a mild-steel plate as a
stamping die set; (2) PCBs (already substantially real -- gerber fab
sets both tiers); (3) perf-board routing (fixed-grid substrate,
jumper/wire assignment, human-followable wiring map + cut list);
(4) dwelling/house wiring (circuits, panels, cable schedules) on top
of the WO-132..137 power track.

Rulings:

1. Every program lands through the AD-47 capability registry -- no
   capability ships without its process records, DFM checks, tool
   adapters (with AD-45 provenance), and claim vocabulary.
2. Prerequisites before the new programs' realizer halves:
   `RealizedLayout` put seam (A7) for anything board-shaped; the
   AD-44/AD-45/AD-46 charter tickets for everything.
3. Wire-EDM program needs: material STATE modeling (heat-treatment /
   hardness as a first-class material state, absent today -- language
   + stdlib work), a profile-cut program kind (2D contour + lead-ins,
   distinct from milling removal ops), die-set assembly composition
   (bolted joints exist, WO-72 precedent), and EDM process records +
   DFM (kerf, corner radii, taper, skim passes).
4. House wiring stays REPRESENTATION-first under the D250/D258
   licensing posture: rule MODELS with cited public-domain/owner-
   verifiable sources; no transcription of NEC tables beyond what
   the existing std.power citations already cover; refusals stay
   named. Breaker/panel content remains gated on verifiable offline
   sourcing (D250 sec. 3) unless the owner supplies sources.
5. Sequencing: charter tickets -> RealizedLayout seam -> perf-board
   (smallest board-shaped win, exercises the new seams end to end)
   -> EDM program (largest language surface) -> house wiring (rides
   power track WO-132..137 completion).

## D269 -- the stdlib process/DFM expansion program

Owner directive (2026-07-19): stdlib gains DFM + process records for
every process family we can honestly source. Rulings:

1. ONE process-record doctrine before population: a `std.process`
   record schema (capabilities envelope: materials, size limits,
   tolerance grades, surface finish, min features, cost drivers,
   lead-class) + a DFM check-set contract per family, both under the
   AD-37 naming/citation law and the AD-47 registry's
   `process_records`/`dfm_checks` fields. Schema first, data second.
2. Provenance regime (the D266 lesson, owner-flagged 2026-07-19):
   US-government/public-domain sources and general-engineering-
   knowledge values with explicit posture markers; citations
   render-confirmed where used (WO-145 machinery); NAMED REFUSALS
   instead of transcribing copyrighted handbook tables. Counsel
   items from D266 remain open and are not re-litigated here.
3. Taxonomy for population waves (each family = records + DFM checks
   + at least one demo-exercised check): subtractive (milling,
   turning, drilling, grinding, honing, lapping, sawing, wire EDM,
   sinker EDM, waterjet, laser, plasma, oxy); sheet (shear, bend,
   punch, stamp, deep-draw, roll-form, spin); casting (sand,
   investment, die, permanent-mold); molding (injection, blow,
   rotational, thermoform, compression); additive (FDM, SLA, SLS,
   DMLS, binder-jet); joining (TIG/MIG/spot/stick, brazing,
   soldering, adhesive, threaded fasteners, rivets, press fits);
   bulk forming (forging, extrusion, rolling, drawing); heat
   treatment (anneal, normalize, quench+temper, case-harden,
   nitride, stress-relieve -- ties into D268's material states);
   surface (anodize, plate, passivate, paint, powder-coat, black
   oxide); PCB (fab, assembly, reflow, conformal); electrical
   installation practice (the D268 house-wiring companion).
4. Population order follows D268 demo value: EDM + heat-treat +
   stamping first (the die-set program consumes them), then PCB
   assembly, then the long tail.

### Amendment to the process program (same day): exhaustive-research results folded in

The frontier-loop research pass (100 processes, 12 families, coverage
proven against DIN 8580's six Hauptgruppen; dossiers in the session
scratchpad, to be converted into the schema ticket's reference pack)
amends the sec. 3 taxonomy: POWDER METALLURGY (press+sinter, MIM,
isostatic) was omitted and is added as a family; the universe also
gains ECM, gear cutting, hydroforming, hemming, incremental forming,
cold heading, swaging, FSW, laser/ultrasonic welding, electroless/
PVD/CVD coating, shot peening, induction hardening, austempering/
martempering, and solution-treat-age (aluminum/titanium hardening --
the steel-centric framing missed it). Provenance reality check: the
population will be predominantly GEK-with-named-refusals (~78% GEK,
~20% carrying at least one named refusal, ~8% with spot-verified
PD-GOV anchors like MIL-H-6875) -- the record schema must therefore
make the GEK posture a first-class, owner-visible marker, not an
apology. Cross-family finding adopted into D268 sequencing: shot
peening enters the EDM die-set chain as recast-layer remediation,
and press-brake/stamping DFM lands with the die-set wave.

## D270 -- materials science program (owner directive, 2026-07-19)

Owner: feldspar should carry everything needed to pick a material or
justify a selection -- phase equilibria (eutectics, alloy systems),
crystal structure, TTT/CCT-class transformation behavior, thermo,
and price. Rulings:

1. MODEL/RECORD split per AD-37: feldspar gets the MODELS (phase-
   diagram thermodynamics, TTT/CCT prediction via published closed-
   form kinetics families -- Kirkaldy/Li-type diffusional onset,
   Koistinen-Marburger martensite, Grossmann ideal critical
   diameter, Jominy correlations, Hollomon-Jaffe tempering --
   each cited, calibrated against independently published oracle
   points); lithos stdlib gets the RECORDS (compositions, crystal
   structures, price classes) as cited community-tier data.
2. Licensing: NEVER transcribe ASM-handbook chart curves; predict
   from published equations instead. Price data from public-domain
   sources (USGS Mineral Commodity Summaries class) as cost CLASSES,
   never scraped vendor quotes. D266 counsel items stay open.
3. Selection/justification surface: a solver route from requirements
   (hardness, toughness class, section size, quench severity, cost
   class) to candidate materials with the full calc chain as
   evidence -- the material-selection justification artifact.
4. Tickets: feldspar T-0018 (filed + pushed 2026-07-19) = model
   half + record schema; the lithos stdlib record-population ticket
   is its companion (filed after the closeout lane releases
   tickets.md). The D268 EDM program's heat-treat states consume
   this program's outputs.

## D271 -- WO-146 ratifies the `.rgp` traced-profile format (T-0017)

Records, as the ratification act (not a re-argument), the D259 sec.
5 structural-boundary ruling this format complies with, verbatim
from `scratch_recon_graphite_cad.md` sec. 5:

> D253 (design log 2026-07-15-cycle-36.md:873-926) forbids the GUI
> from EDITING an existing design's semantics through an override/
> injection channel that bypasses source -- "SEMANTIC (source-only,
> never a GUI): ... dimensions ..." is about a GUI writing semantics
> into DERIVED or RUNTIME state so that the picture and the source
> disagree. It does not forbid the GUI from AUTHORING NEW SOURCE. A
> scan-trace tool whose output is a declarative source artifact --
> written to disk, reviewed by a human as a git diff, committed,
> compiled through the ordinary pipeline with full provenance -- IS
> the source-only path. The GUI is a source EMITTER, like a
> keyboard; it is never a runtime overrider.
>
> THE STRUCTURAL BOUNDARY (each clause is enforceable in code review
> and by tests, not by discipline):
>
> 1. graphite's studio writes SOURCE TEXT ONLY, under the project's
>    source tree, visible to git: the traced-profile file, the
>    pinned scan copy, and a `.hema` snippet. It never writes
>    derived artifacts, ledgers, lockfiles, waivers, overrides,
>    aesthetic view pins, or anything the pipeline reads other than
>    as source.
> 2. graphite talks to the build pipeline in exactly the two ways it
>    already does, and no third: it READS the D244 artifact index,
>    and it drives ordinary CLI builds through the existing run
>    console. Saving a trace does NOT trigger a compile; the user
>    reviews, then builds.
> 3. No new lithos surface accepts data from graphite at runtime.
>    The lithos-side work is a file format + its elaboration in
>    ordinary lowering -- the compiler cannot tell whether a
>    traced-profile file was written by graphite, by hand, or by a
>    vendor. (That indistinguishability is the proof the boundary is
>    structural.)
> 4. v1 writes NEW files and re-writes ITS OWN emitted traced-profile
>    files (round-trip); it never rewrites hand-authored `.hema`
>    text. Even the re-write is a source diff under git --
>    source-only holds.
> 5. D246 untouched: claims, evidence ladder, trust floors, `model=`,
>    `sf=` remain source-only and are not writable from the studio;
>    the studio emits geometry + provenance, never claims.
> 6. Calibration (including the multi-point/grid ladder and the
>    distortion fit) is part of AUTHORING PROVENANCE: the fit runs
>    in the studio before any source exists, its outputs are fields
>    of the source artifact, and the compiler only ever APPLIES
>    recorded parameters deterministically. No runtime channel is
>    created or needed.

The normative document this ratifies: `docs/spec/toolchain/
46-traced-profiles.md` (the `.rgp` schema, calibration ladder,
mandatory provenance, and diagnostics) plus two one-line amendments
naming `rgp` beside `dxf`: `docs/spec/hematite/02-language.md` and
`docs/spec/regolith/11-packages-and-stdlib.md`'s `formats` table
(the surviving home of the L3 external-link row this WO's dispatch
brief cited as `toolchain/08-lowering-architecture.md` sec. 4 --
that filename does not exist in the current tree; the formats
table is its equivalent surface, noted here rather than inventing a
new charter-numbering scheme, per WO-146's own escalation clause).
The trust-tier/method word (`trust_tier = "authored"`,
`method = "traced_scan"`) is settled in `46-traced-profiles.md` sec.
2, in coordination with the D257 structured-citation model, so one
citation family spans stdlib records and traced profiles.

WO-147 (Rust schema + extern-profile elaboration + diagnostics)
implements against this document with zero remaining design
decisions.

## D272 -- cycle-38 schema-bump adjudication (coordinator ruling)

The WO-133 implementer escalated correctly: PowerNetPayload needs a
new wire shape; D211 grants one bump per cycle; WO-147 owned the
CYCLE-37 bump and never landed it. Ruling: the cycle-37 window is
closed history -- cycle 38 has its own single-bump budget, and it is
granted to WO-133 as OWNER (SCHEMA_VERSION 30 -> 31), carrying ONE
named passenger: the FlownetPayload/DischargeRequest claim-target
field (the WO-141 escalation; the 0.0/1.0 direction-encoding
convention retires). WO-147's .rgp types ride the NEXT window --
sequencing-honest, since WO-146's spec ratified only today (D271)
and the graphite write seam (WO-G12) that produces .rgp files has
not landed. Any other schema candidate this cycle escalates; the
bump is spent once WO-133 merges.

### F-WO133-1 (resolved same day, under D272)

WO-132's bus/load lists could not declare the properties the frozen
SCHEMA-31 PowerNetPayload requires. Ruling applied: grammar extended
(optional per-item kwargs mirroring apparatus edge kwargs; cuprite
spec 10 sec. 3a), schema untouched; an omitted required property is
a coded refusal (E0217 POWER_PAYLOAD_FIELD_UNRESOLVED), never a
fabricated default (D250.3). Cross-standard mixing guards as E0216.

## WO-151 close-out -- waveform/mask record class + posture refusal (D263.1)

Landed: the `waveform`/`mask` record class (`regolith/02-quantity-core.md`
sec. 5b; `regolith.magnetite.waveform`), the `authored`/`measured`/
`model_derived` posture taxonomy enforced by unreachable constructors
(`construct_authored`/`construct_measured`/`construct_model_derived`),
name-based registry resolution (`resolve_mask_ref`, hash-pinned,
`model_derived` hashes checked against real calc-sheet digests),
`buck_converter.cupr:34`'s `monotonic_rise(5ms)` given a real
`examples/tracks/cuprite/records/masks.toml` row, and the
`bringup_expectation_authored_posture` refusal wired into
`harness_pack.check_bringup_expectation_authored_posture`.

Two escalations, argued out rather than folded into this WO's own
scope:

1. **E1104 mint deferred (PENDING-CODEGEN).** The dispatch scoped this
   implementer's Rust edits to the `comparison.rs` residual comment
   only; minting the real `regolith-diag::code::BringUp` E1104 slot
   (confirmed free, `code.rs:620-640` shows E1101-E1103 assigned) and
   its `explain.rs` entry needs a follow-up change plus `make codes`
   regeneration. The diagnostic ships today as the bare string
   `bringup_expectation_authored_posture` (`harness_pack.
   BRINGUP_EXPECTATION_AUTHORED_POSTURE`), the same pre-D247.2 shape
   `EXPECTATION_PROVENANCE_UNRESOLVED` itself had before its own
   backfill -- no grandfathering once the real code lands, per WO-131
   house law. Reopen criterion: the follow-up Rust change lands E1104
   + its explain entry; `regolith explain E1104` then prints the real
   entry this WO's acceptance criterion wants.
2. **Quantity/axes dimension check deferred to the compiler core.** The
   spec (`02-quantity-core.md` sec. 5b) assigns the `mask=` ref's
   quantity/axes-vs-claim-subject dimension check to regolith-qty (the
   Rust compiler core); `resolve_mask_ref` (Python, `regolith.
   magnetite.waveform`) resolves the NAME and, for `model_derived`,
   the calc-sheet hash, and returns the record's own declared
   `quantity`/`axes` for a compiler-core caller to dimension-check --
   only `regolith.compiler` may import `regolith._core` (AD-4), and
   this WO's Rust-edit scope did not include a new compiler-core
   integration. Reopen criterion: a follow-up WO wires
   `resolve_mask_ref`'s output into the claims-lowering pipeline's
   dimension check, closing the gap between "the ref resolves" and
   "the ref's dimension actually matches."

Post-completion feature audit (D260 ruling 2): a `kind = tolerance`
convenience constructor is not obviously missing yet -- no corpus mask
today declares `kind = "tolerance"`; deferred, reopen on the first
real tolerance-shaped mask request. Other named `mask=` refs in the
corpus beyond `monotonic_rise` were not censused in this WO (out of
scope: this WO's deliverable 6 named exactly one backfill); a corpus-
wide census of unresolved `mask=`/`stimulus` refs is a good small
follow-up WO, not folded in here.

## WO-152 close-out -- waveform/mask rendering + AUTHORED badge (T-0022)

Landed: `regolith.backends.drawings.producers.waveform_chart` projects
a `WaveformMaskRecord` (WO-151) into a real axes-with-ticks chart
through the SAME chart code path `opt_trace` already uses
(`_CHART_SOURCE_KINDS` widened in `renderer.py`/`renderer_pdf.py` to
recognize a `waveform.record` view alongside `optimize.trace` -- one
renderer, AD-7/charter 41 rule 6, never a second one). The AUTHORED
badge (D260 ruling 3) is a chart annotation driven strictly by the
cited record's own `provenance.posture` field
(`_waveform_authored_annotation`): `authored` gets an unmistakable
`AUTHORED (design intent)` annotation (its own `authored-badge` CSS
class in SVG); `measured`/`model_derived` gets none, proven by golden-
style byte-diff tests (`tests/backends/test_waveform_chart.py`). The
mask-overlay case (a claim's own discharge chart showing the mask it
must `stays_within` on the SAME axes) generalizes `_chart_polylines`:
a view's `SegmentEntity` chain now splits into multiple disjoint
polylines wherever a real discontinuity occurs, so a signal series and
a mask series drawn in ONE view render as two strokes on one chart
rather than one falsely-joined zigzag -- backward compatible by
construction, since every existing continuous-chain producer
(`opt_trace`) never has a discontinuity and still yields exactly one
polyline (pinned by
`TestOptTraceUnaffectedByWaveformChanges`).

Scope note: T-0022's own `scope:` field names `python/regolith/
emission/**`, a directory that does not exist in this tree; every
sheet/record renderer this WO extends lives under `python/regolith/
backends/**` (`drawings/producers.py`, `drawings/renderer.py`,
`drawings/renderer_pdf.py`), which is where the WO body itself points
and where `frob_check_scope T-0022` reports clean. Flagged here so the
ticket's `scope:` glob gets corrected in a follow-up bookkeeping pass
rather than silently left to drift.

Landed deliverable 5 at the function level:
`harness_pack.bringup_waveform_view(tap, expected_value, expected_units,
record, overlay=mask)` renders the SAME chart component beside a
"Tap N" table (channel/target/expected) on one sheet
(`tests/backends/test_waveform_chart.py::TestBringupWaveformView`).

Deferred, not landed in this WO:

1. **Pipeline wiring + demo regeneration (deliverable 5, partial).**
   `bringup_waveform_view` is not yet called from `harness_files`'s
   file-emission loop for every tap whose `expected_signals` row cites
   a `mask=`/waveform ref, and `demos/out/demo17_physical_bringup_pack/`
   is not regenerated to show it -- that plumbing pass through
   `harness_files`'s `Tap`/`ExpectedSignal` iteration did not fit this
   WO's time-boxed implementer session. Reopen criterion: a follow-up
   ticket calls `bringup_waveform_view` once per tap whose
   `expected_signals` row's provenance resolves to a `record` ref, and
   regenerates the demo17 fixture (or an equivalent new demo) as its
   own golden change.
2. **Multiple masks over one window.** Post-completion feature audit
   (D260 ruling 2): `waveform_chart` accepts exactly one `overlay=`
   today; a claim citing MULTIPLE masks over the same window would
   want all of them overlaid together. No corpus fixture needs this
   yet (`monotonic_rise` is the only wired mask). Argued out for now;
   reopen on the first real multi-mask claim, generalizing `overlay`
   to a tuple.
3. **AUTHORED-badge convention on other provenance-posture artifacts.**
   The badge convention (posture-field-driven annotation, distinct CSS
   class) is specific to `WaveformMaskRecord` here. Whether it should
   extend to any other provenance-posture-carrying artifact type in
   the fleet is argued out for this WO (no other artifact family
   declares a D263.1-shaped posture field today); reopen if one does.

