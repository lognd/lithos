# Design log -- 2026-07-16, cycle 37

Cycle 37 opens on the cycle-36 seed queue plus three owner directives
delivered 2026-07-16: (1) a DigiKey-seeded processors extension to the
stdlib with full datasheet fidelity and citations for everything;
(2) research toward thermodynamic/fluid depth (Moody-chart territory)
aimed at an end-to-end demo to real people soon; (3) graphite gains
profile authoring -- scan, size via reference points, trace, basic
CAD -- tied into the pipeline, with the declarative path remaining
first-class. The owner granted full autonomy for the working session
of 2026-07-16 ("do everything autonomously"); design decisions below
are coordinator-made under that grant and argued here as usual.

The D256 hash window (the resolve_unit_suffix unit-token repair +
wholesale regeneration) executes FIRST this cycle, serialized: no
other lithos code work lands until it merges. Its own log section
arrives with its integration.

## D257. DigiKey is discovery-only; committed parametrics come from manufacturer datasheets under structured citations

The directive asked for a "digikey extension" carrying every value in
the parts' datasheets. The DigiKey API User Agreement forecloses the
literal reading: sec. 5.1(e) forbids using the API or DigiKey Data to
"update or create your own database of information", 5.1(d) forbids
bulk download, and 3.2(iv) forbids redistributing DigiKey Data. A
committed stdlib corpus populated from the API is exactly the
prohibited database, in a public repo. The owner confirmed the split
(2026-07-16): DigiKey serves DISCOVERY ONLY, at runtime, through a
live loader whose output is attributed to DigiKey (sec. 3.1.4) and
never committed; ALL committed parametric data is transcribed from
MANUFACTURER DATASHEETS, which are not DigiKey Data, under per-value
citations. The two loaders stay separate BY CONSTRUCTION so no API
response can reach the repo. This is recorded as law: any future
design that caches DigiKey API responses into the tree is a violation,
not an optimization.

Rulings from the processors recon (scratch_recon_digikey_processors.md,
2026-07-16), adopted:

1. Processor catalogs are vendor-named record packages (ti.mcu,
   microchip.mcu, ...) beside the existing ti.logic / st.mcu /
   microchip.cpld -- the ordinary magnetite mechanism, never a new
   plugin kind, never the AD-26 seam (which is behavior, not data).
2. Citations become STRUCTURED: the one justified divergence from the
   evidence = { method, trust_tier, reference } house shape is
   decomposing the prose reference string into fields (document,
   revision, date, page, table, url), so the charter 39 sec. 5.4
   health check can tighten from "string non-empty" to "page cited,
   doc/rev known". The per-field precedent already exists:
   transformer_dry_type's xr_ratio_evidence (D224). Uncited datasheet
   values become unrepresentable at the model level (no constructor
   without a citation).
3. Datasheet ingestion is HUMAN-IN-THE-LOOP transcription with a
   machine assist, confirmed against the RENDERED page (the WO-134B
   lesson, independently reproduced by the recon's own prototype
   misreading a wrapped VCC+0.3 abs-max bound). No automated corpus
   factory; a mandatory confirmed gate rides every generated row.
4. First slice: the citation models + health tightening, then ONE
   package (ti.mcu), ONE family (MSP430FR5, TI SLASE54D Rev. D),
   uniform core only (~30-60 cited values per part), tier=community.
   Assigned WO-145 (after the fluids block, D258 ruling 4).

Owner items parked: ST.com bot-wall (browser fetch likely clears it;
no workaround attempted per house rule), DigiKey credentials live in
../digikey/.env only, optional pdfplumber dev dependency, and making
explicit the de-facto rule that community-tier rows may carry
confirmed=true on transcriber+review.

## F158. The fluid corpus promises properties it does not have

The thermo/fluids recon (scratch_recon_thermo_fluids.md, 2026-07-16)
censused the gap between what the corpus declares and what the stdlib
carries. Six registry(...) names resolve to NO record (egw_60_40,
egw_50_50, semisynthetic_5pct, gasoline_e10_summer, grundfos_ups32's
curve, SAE_10W40's full form); media rows that do exist are single
state points while corpus comments promise rho(T)/mu(T)/pv(T);
friction factors are hand-declared magic numbers (thermosiphon.fluo:76
declares friction_factor=0.03, documented elsewhere as a "Moody-chart
estimate folded upstream"); and no pipe-roughness table exists
anywhere in the tree. Every one of these is currently papered over by
a memo waiver -- the small_office hydronics story runs on five of
them. The gap census is the demo plan's spine: each waiver names
exactly the record or model that discharges it.

## F159. Feldspar's fluids core is complete and unreachable

The same recon found feldspar already ships the correlation core the
fleet needs -- Colebrook-White (Newton, Haaland-seeded), Haaland,
laminar 64/Re, Darcy dp, K-factor dp, series/parallel composition,
pump operating point, NPSHa, Joukowsky, and a Hardy-Cross network
solver that already consumes the lithos FlownetPayload -- and NOT ONE
fluids model is wrapped into the regolith pack. The F126.1 waiver
family (fluids.mdot / fluids.flow_imbalance across the fleet) is one
pack-bridge WO away from discharging for real. This is the
highest-leverage single slice in the fluids track and it is
feldspar-side, so it does not wait for the D256 window.

## D258. The fluids track: representation, licensing posture, and WO-138..144

Rulings from the recon, adopted:

1. REPRESENTATION: property data enters as cited point tables when
   the source is a published formulation evaluated on a grid (IAPWS
   -- the polymer_melt D182 precedent), and as coefficient rows ONLY
   when the source itself publishes coefficients (NASA Glenn cp
   polynomials). The repo never invents a fit; lookup brackets
   conservatively per INV-9. One citation model across the stdlib:
   the same structured shape as D257 ruling 2.
2. LICENSING HONESTY: Crane TP-410 and Idelchik are proprietary
   publications; bulk transcription of their K/f_T tables is
   license-blocked pending an explicit owner decision, exactly as the
   DigiKey question was handled. The track proceeds WITHOUT them:
   Hooper 2-K (Chem. Eng. 1981), Darby 3-K (1999/2001), and
   Borda-Carnot textbook geometry laws cover the demo set under the
   same tier=community transcription posture as the NEC tables.
   IAPWS-IF97 and NASA Glenn coefficients are verified free/public.
   Textbook-restatement citations, where a primary is paywalled,
   remain acceptable (existing house practice, now explicit).
3. TRANSITION HONESTY: the friction model declares the laminar-
   turbulent transition indeterminate (D97 regime tags) rather than
   interpolating through it; Haaland-vs-Colebrook deviation is folded
   into the stated tolerance rather than hidden.
4. WO NUMBERS ASSIGNED: WO-138 property-depth records; WO-139
   friction-factor model + roughness records; WO-140 minor losses
   (Hooper/Darby/Borda-Carnot); WO-141 feldspar fluids pack bridge
   (feldspar half dispatched 2026-07-16, ahead of the window --
   feldspar-side only); WO-142 heat-transfer correlation growth;
   WO-143 Moody calc-sheet figure (log-log axes per charter 41 rule
   6, operating point marked with its obligation id, through the ONE
   renderer); WO-144 fluid demo close-out (small_office waiver
   burn-down + demo run with PROOF.md). Dependency: 138 -> 139 ->
   {140, 141-lithos-half, 143} -> 144; 142 parallel after 138; the
   regime/fill half of 144 gates on the F157 claim-shape slice.
5. THE DEMO STORY, adopted as the cycle's presentation target: "size
   a building's hydronic heating loop and hand the reviewer a calc
   book" -- small_office discharges dp/balance/npsh for real, and its
   calc sheet carries a Moody chart with the operating point plotted.
   Fallback if the bridge slips: espresso brew-line dp with derived f.

Owner items parked: Crane/Idelchik bulk posture, Dow guide vs ASHRAE
for glycol data, SAE J300 / ASTM D4814 posture, manual download of
the Grundfos UP-series curve booklet (fetch-blocked twice).

## D259. Graphite may author source; it may never edit semantics

The owner directive (scan -> size via reference points -> trace ->
basic CAD -> pipeline) lands against D253's law that semantic edits
stay source-only forever. The ruling that reconciles them, made
before the design recon reports so the boundary is fixed while the
design is drawn:

D253 forbids the GUI from EDITING an existing design's semantics
through an override or injection channel that bypasses source. It
does not forbid the GUI from AUTHORING NEW SOURCE. A trace tool whose
entire output is a declarative source artifact -- written to disk,
reviewed by a human, committed, compiled through the ordinary
pipeline -- IS the source-only path; the GUI is a source emitter, a
keyboard with better ergonomics, never a runtime overrider. The
boundary is structural, not procedural: graphite's only contacts with
the pipeline are (a) reading the D244 artifact index and (b) saving
source text that the user then compiles. Any design element that
needs a runtime channel from GUI into the compiler is a violation and
an escalation, not a trade-off.

A traced profile is CITED GEOMETRY: its provenance record carries the
scan (file hash, capture date), the calibration (reference points,
known distance, residual error), and the tracer (human, date), in the
same structured-citation family as D257 ruling 2. A trace without its
scan provenance is as unrepresentable as a datasheet value without
its page. The design recon (in flight) fills in the source
representation, the CAD v1 closed set, and the WO decomposition under
this ruling; the declarative non-GUI path remains first-class -- every
GUI action must have a readable source-text equivalent, and the GUI
must be able to reopen its own emitted source (round-trip as a design
requirement, not a feature).

Owner addition (same day, mid-window): calibration must climb past
two reference points to a full ladder -- uniform scale (2 points),
projective homography (4 points, closed-form), and an N-point grid of
known pitch that back-calculates perspective PLUS non-linear lens
distortion (the Zhang planar-target formulation). The provenance
record carries which model was fitted, the grid spec, the parameters,
and the reprojection residual in mm at the object plane; the source
artifact stores image-space points and the calibration transform
SEPARATELY so a trace is auditable and re-calibratable without
re-tracing. A trace whose calibration rung cannot support a consuming
claim's accuracy is a diagnosable condition, not a surprise.

## D260. Authoring surfaces: where hand-writing source is awkward though possible, graphite may grow a front-end that emits it

Owner directive (2026-07-16): signal design should get a front-end
tool, and more generally "any point where it would be awkward --
ALTHOUGH POSSIBLE -- to hand-write" the source may earn one. The
ruling that generalizes D259 into a family:

1. An AUTHORING SURFACE is a graphite tool that emits declarative
   source a human could have typed. The D259 boundary is inherited
   verbatim and is the admission test: the tool writes source files
   and reads the artifact index; nothing else touches the pipeline.
   The emitted source is the unambiguous artifact (the mantra order
   is preserved: the GUI adds user-friendliness ON TOP of an
   unambiguous text form, never a second meaning); round-trip --
   the tool reopens its own emitted source -- is a requirement on
   every surface, not a feature.
2. NO BLIND FEATURES (owner's phrasing, adopted as process): each
   surface gets a design recon arguing every interaction against the
   mantras before implementation, and each surface's WO ends with a
   POST-COMPLETION FEATURE AUDIT -- a recorded design-log pass over
   what small adjacent affordances the finished tool now makes
   obvious, each argued in, deferred with a reopen criterion, or
   argued out. Feature ideas discovered mid-build are logged, not
   slipped in.
3. EVIDENCE HONESTY (the D246 interaction, load-bearing for
   signals): an authoring surface can only emit source whose
   evidence posture is AUTHORED. A hand-drawn expected waveform is
   design intent -- trust-tier authored/asserted -- and can never be
   dressed as a model-backed or measured expectation; the bring-up
   pack's expected_signals carry D224 provenance precisely so a
   drawn curve cannot forge it. The GUI cannot mint evidence it does
   not have, by unreachability, same as D246's "cannot forge a
   pass".
4. First instances: profile/scan tracing (D259, recon in flight) and
   SIGNAL DESIGN (waveforms, envelopes, masks, stimulus/expectation
   pairs -- the concrete owner ask). A census of further candidates
   (where the corpus shows hand-written source straining) belongs to
   the signal-design recon, prioritized by demo value, each with an
   honest "is text actually awkward here, or merely unfamiliar"
   check so surfaces are not built for their own sake.

## D261. Scan-trace adoption: the .rgp extern profile, the calibration staging, and WO-146..149 / WO-G11..G14

The graphite scan-trace recon (scratch_recon_graphite_cad.md,
2026-07-16) reported under D259/D260 and its design is ADOPTED with
one renumbering: it proposed lithos WO-138..141 without sight of
D258's assignment of those numbers to the fluids chain (its worktree
predated the log). The scan-trace lithos slices are WO-146 (spec +
.rgp format ratification, S, docs-only, not window-gated), WO-147
(Rust schema + extern-profile elaboration + diagnostics, L, AFTER the
D256 hash window -- it owns the cycle-37 SCHEMA_VERSION bump, D211
discipline: one bump, one owner), WO-148 (Python realizer/citation/
artifact-index, M, after 147), WO-149 (native-walk promotion, v1.5,
unscheduled). Graphite-side numbering stands: WO-G11 (studio
substrate + calibration rungs A/B + grid capture), WO-G12 (trace
tools + .rgp emission + the one new source-tree-only write endpoint),
WO-G13 (round-trip/pipeline UX), WO-G14 (rung-C k1/k2 distortion
fit, validation-first, v1.1).

Rulings adopted with the design:

1. The trace enters through the ALREADY-SETTLED extern seam
   (hematite 02's extern profile slot): a transparent TOML format,
   schemars-single-sourced, storing IMAGE-SPACE points and the
   calibration transform SEPARATELY; elaboration applies the
   transform deterministically. Provenance is a required schema
   field -- uncited geometry is unrepresentable. The compiler cannot
   distinguish a graphite-emitted .rgp from a hand-written one;
   that indistinguishability is the D253/D259 compliance proof.
2. Calibration ships rungs A+B in v1 (uniform scale; DLT homography
   -- closed-form, dependency-free); rung C (radial distortion fit)
   is v1.1 as WO-G14, and the staging costs nothing because grid
   observations are captured from day one: any trace re-calibrates
   to a better rung WITHOUT re-tracing. Coordinator accepts the
   staging argument; the owner may pull rung C into v1 on return.
3. Edge-detect assist is deferred; v1 traces manually with snapping
   and per-point confirmation, matching the house transcription
   posture. The evaluated library option (OpenCV.js, Apache-2.0,
   ~8MB bundled) is recorded for the reopen.
4. pitch_basis enters the calibration provenance record: a printed
   paper grid's pitch error (0.2-0.5%) dominates the accuracy
   budget and must be declared, not assumed away. Choosing a
   measured/certified house grid target is an OWNER item.
5. The trust-tier/method vocabulary word for traced geometry is
   settled inside WO-146 in coordination with the D257 structured-
   citation model -- one citation family across stdlib and traces.
6. Demo story adopted: espresso_machine's group gasket on a 10mm
   grid -- scan, confirm intersections, trace, export, narrate the
   git diff, build from the run console, calc sheet cites the scan
   hash and rms residual. Minimum set: WO-146/147/148 + WO-G11/G12.

Owner items parked: rung-C pull-forward, the physical grid target,
the graphite charter 3.2 authoring carve-out wording (rides WO-G11).

## D262. No bare dimensioned values in artifacts -- enforcement by unreachability, not by sweep alone

Owner directive (2026-07-16, mid-window): "we had bare values in
artifacts; make sure that we enforce units to stop any dumb mistakes
... no dumb mistakes should EVER be possible to make."

The observed bare values are F156's family -- resolve_unit_suffix
discarding the unit token -- and the ROOT FIX is already executing in
the D256 hash window. This ruling is about the second half of the
ask: after the window closes, bare-ness becomes IMPOSSIBLE rather
than currently-absent. By D256's own law nothing rides in the window,
so this is the first post-window slice, assigned WO-150:

1. STRUCTURAL HALF (the real enforcement): every artifact-rendering
   interface that accepts a dimensioned value accepts a unit-carrying
   quantity type (regolith-qty Qty/Interval or the schema mirror),
   never a bare float-plus-hope. The renderer signature refuses the
   mistake at the type level, the same unreachability doctrine as
   D246 ("cannot forge a pass") and D257 ruling 2 (uncited values
   unrepresentable). Genuinely dimensionless values pass an EXPLICIT
   dimensionless unit, not an absent one -- absence stops being
   expressible.
2. SWEEP HALF (the rot guard): a health-consistency sweep over
   emitted artifacts (calc sheets, bring-up packs, drawings, BOM
   views) that flags dimensioned-looking bare numerals, as a
   regression tripwire for surfaces the type system cannot reach
   (e.g. prose strings). Report-only at first landing, promoted to
   error once the fleet is clean, per the F154 lesson applied in
   reverse: a gate promoted before it is satisfiable is a gate that
   gets waived.
3. The guarantee enters the invariant ledger with its proof argument
   in the SAME change (INV number coordinator-assigned at
   integration; the proof argument is ruling 1's unreachability plus
   ruling 2's sweep evidence).
4. Seam notes: the signal-design recon (D260) already carries the
   axis-unit discipline for authored waveforms; WO-145's Cited
   models carry units inside cited values; WO-147's .rgp schema
   declares mm explicitly. New surfaces inherit this ruling at
   design time -- a reviewer may reject any new interface that
   accepts a dimensioned bare float as a matter of law, citing this
   entry.

WO-150 depends on the D256 window (it builds on the repaired unit
channel) and precedes WO-143 (the Moody sheet renders quantities
through the enforced interfaces rather than being migrated later).

## D263. Signal-design adoption: the waveform/mask record home comes first, and the GUI cannot construct evidence

The signal recon (scratch_recon_signal_design.md, 2026-07-16,
reporting under D260) is ADOPTED with renumbering (it proposed
numbers D261 had already assigned): lithos WO-151 (waveform/mask
record class + spec section + the refusal diagnostic; M; after the
D256 window) -> WO-152 (calc-sheet rendering + AUTHORED badge; S);
graphite WO-G15 (read-only waveform view; S) -> WO-G16 (the editor +
emitted records; M/L). The graphite WRITE-SOURCE SEAM is built ONCE,
owned by the scan-trace track (WO-G12, the first implementer), and
WO-G16 consumes it -- two surfaces, one seam.

Rulings adopted:

1. The decisive survey fact: regolith/02 sec. 5 and cuprite/03 sec. 7
   already define masks/windows/transients and waveform profiles, and
   the corpus already references named masks by hash -- but the DATA
   has no landed home (the comparison.rs from_table residual). The
   surface's prerequisite is therefore a RECORD CLASS (class =
   "waveform" | "mask" TOML records, axis units declared once,
   spec-first), not a grammar change.
2. v1 = piecewise-linear/step waveform editor + envelope/mask tool,
   live source pane, byte-stable round-trip. The recon's cuts stand
   as argued, each with its reopen criterion (clocks: three numbers
   beat a drawing; bus tables: compact text superior, reopen at
   >100-line hand tables; stimulus channel: gated on a charter 37
   amendment, owner call; splines: not unambiguous; from_fn
   families: standards are transcribed, never drawn) and one
   PERMANENT cut with no reopen: in-GUI claim editing (D246/D253).
3. EVIDENCE POSTURE adopted as proposed: a provenance field
   posture = authored | measured | model_derived, orthogonal to
   trust tier. The GUI emit path can only CONSTRUCT authored;
   measured requires instrument fields; model_derived is
   unconstructible without a resolving calc-sheet hash only the
   pipeline holds. The bring-up expected-signal resolver never walks
   records (unreachability), and citing an authored record where an
   expectation or model= pin is required raises the new E11xx
   diagnostic bringup_expectation_authored_posture. This is D260
   ruling 3 made concrete.
4. The awkwardness census verdicts stand: waveform/mask proceeds
   (with the honest note that short masks are fine as text and the
   surface's real value is visual verification); cuprite nets bind
   lists and calcite node coordinates DEFER with their stated reopen
   thresholds; fluorite nets and datasheet transcription are ruled
   out (unfamiliar-not-awkward / a different tool family).
5. The measured-trace importer stays yoked to charter 40 sec. 6's
   live-capture reopen; the posture taxonomy ships complete from day
   one regardless, so the importer lands into a slot that already
   refuses imposture.

Owner items parked: the charter 37 stimulus-channel amendment
(joins this cycle only on owner say-so), final record-class spelling
(settled inside WO-151 with the D257 citation family).

## D264. The cuprite sim/timing gate: sibling gates on an in-house process seam, WO-153..158

The sim-gate recon (scratch_recon_cuprite_sim_gate.md, 2026-07-16)
is ADOPTED. Its survey corrected two assumptions worth recording:
"DFM must-pass" is not a dedicated invariant but the composition of
INV-24's acceptance ledger, error-level rule diagnostics, and the
health-fleet census golden's discharged-decrease refusal -- so the
cuprite gate clones THAT composition, not a lookalike flag; and
simulation vs timing are SIBLING gates (verilator is functional
only; timing data exists nowhere in code today -- budget kind=timing
parses and nothing more), so they land as separate slices rather
than one pretending to be both.

Rulings:

1. PROCESS SEAM, in-house: regolith.procio, adopting lograder's two
   proven ideas (typed-argv models, invocation-as-data with
   mandatory timeouts) on typani Result + toolenv, WITHOUT depending
   on or vendoring lograder (its non-typani Result, auto-install
   posture, ambient config, and dependency drag argued against;
   both repos are owner-owned so "in-house" was satisfiable either
   way -- the tiebreaker is contract fit). Six shipped call sites
   migrate; the AD-19 wire protocol is kept verbatim; tools/health
   stays raw (recorded exception). The owner's validation bar is
   the seam's reason to exist: no bare subprocess.run anywhere in
   shipped code after migration. Owner may override the
   dependency-posture call on return.
2. GATE ECONOMICS as designed: sim discharges content-address
   cached on (src digest x stimulus digest x model version);
   make check keeps only the existing fixture sims; the health
   fleet leg runs full cuprite sims bounded by seam timeouts; the
   census discharged-decrease rule is the anti-erosion tripwire.
   Determinism: directed hash-pinned vectors in v1, tool version
   recorded in evidence, known_good version FLOOR (warn on drift)
   rather than a hard pin. F154 compliance: every cache/exemption
   key is a digest/claim-kind/project-name, never a line number.
3. AUTO-EMISSION STRENGTH, v1: declared-stimulus simulation plus
   named-absence coverage rows. Refusing release for every
   uncovered behavioral subject would instantly red the fleet
   against D206's ship-green law; coverage tightening is the
   fleet-adoption WO's burn-down, not a day-one cliff.
4. SCHEMA DISCIPLINE echoed: if the signal_table payload needs a
   wire-schema change it rides WO-147's single cycle-37 bump or
   sequences after it. Never a second bump.
5. VCD/waveform RENDERING is the signal surface's job (D263); the
   named seam between the gates and that surface is the
   signal_table payload with posture-carrying provenance -- an
   authored waveform may DRIVE a sim verdict but can never claim
   model-backed tier (D260 ruling 3).
6. setup_slack / corners(all) language surface: deferred with the
   recon's reopen criterion; timing v1 closes generic budgets over
   WO-145's cited record values + route delays via the existing
   E0432 path.
7. WO NUMBERS ASSIGNED: WO-153 procio seam (S, first); WO-154 spec
   deltas + the invariant text with proof argument (M, docs,
   parallel); WO-155 the sim gate (L); WO-156 timing closure v1
   (M, after WO-145); WO-157 fleet adoption + waiver burn + census
   flip (M); WO-158 riscv sim demo + expected_signals cross-check
   (S). All post-D256-window.## D265. The D256 hash window executed: the unit token rides the text, not the schema

Executed the D256 ruling (`2026-07-15-cycle-36.md`, F156/D256) as its
own dedicated cycle-37 hash window: the `resolve_unit_suffix` repair,
the interim closed-SI-vocabulary channel's deletion, and the wholesale
golden/ledger/waiver-hash regeneration this repair forces. Nothing
else rode along.

### The fix

`crates/regolith-lower/src/claims/common.rs::resolve_unit_suffix`
parsed a declared bound's unit through `regolith-qty`'s `Unit::
parse_expr` for its SI scale factor and then discarded the token
(`45ohm` -> `45`). It is the ONE helper all 14 call sites route
through (comparison bounds, window halves, sweep domains, temporal
window durations, `floor(...)` masks), so the fix lands once.

`Unit` gained one new method, `base_symbol()`
(`crates/regolith-qty/src/unit.rs`): the canonical unprefixed SI
symbol for a unit's dimension, found by matching the unit's dimension
against the fixed unit table's unprefixed, non-offset rows (`mV` ->
`V`, `kN` -> `N`, `ohm` -> `ohm` unchanged; a compound dimension the
table names no single entry for, e.g. `N/m`, returns `""` -- the
caller omits the suffix rather than fabricate a spelling the crate
would not itself accept back as input). `resolve_unit_suffix` now
appends this symbol after the SI-reduced magnitude instead of nothing:
`45ohm` stays `45ohm`; `20mV` reduces to `0.02V` (the BASE symbol,
never the original prefixed spelling, which would mislabel the
reduced value); `degC` resolves through its additive offset and
carries `K` (its dimension's base unit); a suffix `regolith-qty` does
not recognize (`rpm`, `dB`) is untouched exactly as before.

### Representation choice (documented per the dispatch brief)

Two shapes were on the table for the lowered obligation text:

1. **Attach the unit directly to the magnitude** (`45ohm`, chosen).
   Every existing Python-side bound reader already treats a trailing
   unit token as its PRIMARY shape: `_resolve_bound`/`_split_bound_term`
   (WO-122, `translate.py`) already parse an attached-or-detached unit
   suffix off a bound's text, because a unit `Unit::parse_expr` does
   not recognize (`rpm`, `%`, `dB`) was ALREADY passing through this
   way pre-D256. `unit_from_claim` (`backends/calc.py`) and this WO's
   own `_split_expected_magnitude_and_unit` (`backends/harness_pack.
   py`) are the same shape. Choosing this representation meant ZERO
   Python-side parsing changes for the bound-resolution path itself --
   only the two closed-vocabulary FALLBACKS (now dead code paths since
   the primary path always succeeds) needed deletion.
2. A separate structured unit field on the obligation/claim schema
   (an L3 payload bump) -- REJECTED: out of this window's scope by the
   dispatch brief's own instruction (a schema bump requires escalation
   and D256.3 already ruled the cycle-36 D239 schema window stays
   closed and unspent; nothing in this window needed a new field
   because the existing text-based readers already tolerated this
   shape).

No schema bump was needed or taken. The L3 payload schema is
byte-identical before and after this window.

### A real bug this exposed and fixed in the same commit

With the unit now attached to the claim's own `rhs`, the evidence
surface's `expected` (bare-value) and `units` (unit-only) fields would
otherwise both carry the unit, doubling it in `bringup.md`'s
`"expect <value> <unit>"` line (`45ohm ohm`). `harness_pack.py`'s
`_expected_text`/`_units_of` pair (WO-128) is replaced by one function,
`_expected_magnitude_and_units`, that splits the claim's declared
threshold into magnitude and unit ONCE -- `expected` stays the bare
magnitude the L3 schema already commits to, `units` stays separate.
Caught by the existing unit test fixture (`_rail_obligation`'s
`rhs="3.465V"`), not discovered live -- the pre-existing `elec.
impedance` corpus claim happened to have its unit stripped by the bug
this window fixes, so the duplication was latent until now.

### The interim closed-SI-vocabulary channel, deleted (per ruling 4 of the window's charter)

WO-128 (cycle 36) landed a fallback -- `translate.si_output_unit` +
`_SI_TERMINATION_UNIT`, a hand-maintained `(scheme, selector) -> unit`
table for `elec.impedance`/`elec.termination` -- because the Rust bug
this window fixes made the claim's own unit token unreachable. D256.4
required its deletion once the root fix landed, since two live unit
channels is exactly the "no duplication" violation the project treats
as a bug waiting to desync.

Deleted: `si_output_unit`, `_SI_TERMINATION_UNIT`,
`si_sheet_fields`'s `"unit"` dict key (translate.py);
`calc.py::_si_unit_from_obligation` and its position in `_build_sheet`'s
unit-fallback chain; `harness_pack.py`'s `si_sheet_fields`-based
fallback branch inside the old `_units_of`.

Proof exactly one channel remains (per the acceptance criterion):

    $ grep -rn "def si_output_unit\|_SI_TERMINATION_UNIT\|def unit_from_claim\|def _split_expected_magnitude_and_unit" python/ crates/
    python/regolith/backends/calc.py:447:def unit_from_claim(claim: Claim) -> str:
    python/regolith/backends/harness_pack.py:95:def _split_expected_magnitude_and_unit(text: str) -> tuple[str, str]:
    python/regolith/orchestrator/translate.py:2649:    closed-SI-vocabulary table (`si_output_unit`/`_SI_TERMINATION_UNIT`)

(the `translate.py` hit is a historical comment naming what was
deleted, not a live symbol.)

mainboard_mx's bring-up still reads `expect 45 ohm` citing its calc
sheet (`tests/backends/test_harness_pack.py::
TestBuildExpectedSignals::test_discharged_si_claim_prints_real_value_
with_units`) -- via the root fix now, not the interim channel; the
fixture's claim `rhs` is `"45ohm"` (the D256 shape) and the test
asserts `expected == "45"`, `units == "ohm"` separately.

### Wholesale regeneration: reviewed, pure hash motion

Rust unit tests updated in place (14 assertions across
`crates/regolith-lower/src/claims/tests.rs`) to expect the preserved
unit token; six new unit tests added directly for `resolve_unit_suffix`
(`common.rs`) and two for `Unit::base_symbol` (`regolith-qty/src/
unit.rs`). Full Rust workspace test suite: green, no other test
touched.

Python goldens regenerated via the sanctioned `REGOLITH_UPDATE_GOLDEN=1`
targets only (`tests/golden/test_golden_corpus.py`,
`test_calc_corpus.py`, `test_deferral_corpus.py`,
`tests/backends/test_si_sheet.py`) -- nothing hand-edited. 27 golden
files changed. Diff reviewed field-by-field before committing:

- Every corpus golden's `diagnostic_multiset`/`obligation_count`/
  `snapshot_count` is byte-identical across all 21 project corpora;
  only `obligation_keys` (content hashes) moved -- 492 hash slots
  total across the corpus.
- `deferral_cnc_router.json` (179 rows) and `deferral_dune_buggy.json`
  (225 rows): identical deferral-reason multiset before/after -- zero
  deferrals added, removed, or reclassified.
- `calc_book_cnc_router_r1.json`/`calc_book_timber_pavilion.json` and
  their `calc_audit_*` pairs: identical
  `(claim_name, disposition, subject_anchor, detail)` multiset for
  every waiver row, identical summary -- pure hash motion, zero
  waiver-set membership change.
- `si_sheet_si_board.json`: all 9 rows stay `discharged`; the only
  content change is the `target` field now carrying its unit
  (`">= 45"` -> `">= 45ohm"`) -- the visible fix.

Zero added or removed error-level rows anywhere in the regenerated
corpus. Zero waiver-set membership changes. Zero verdict movement.
`make check` (after `make install`): green, `HEALTH: PASS`, fleet
census golden matches with 0 mismatches / 0 rigor regressions.

### Demos: restored, not committed (D256's note 7)

`demos/out/` churned on every `make check`/pytest run during this
window, as documented (manifest byte counts, PDF/gerber hashes). Some
of that churn (calc_book.json/bringup.md/expected_signals.json content
shifting for demo15/demo17/demo6) traces to this window's fix; some
(demo11's gerber-only diffs, demo16's `config_precedence.txt`, which
bakes in this session's absolute worktree path) does not and is
unrelated environment noise. Because the two are entangled in the
same generated files and cannot be cleanly separated without running
a dedicated demo-regen pass (out of this window's scope), ALL
`demos/out/` changes were `git restore`d rather than committed. A
follow-up demo regen (in-scope for whichever window next touches the
fleet corpus, or its own small chore) will pick up the real content
shift.

### Escalations: none

No schema-bump need found. No regression rows found. No waiver-set
membership changes found. No adjacent bugs found beyond the
`expected`/`units` duplication fixed in the same commit (that one was
IN scope: it is a direct consequence of re-pointing consumers per
D256.2, not a drive-by).
Coordinator review addendum (at integration): the window's report
claimed identical deferral-reason multisets; that claim covered the
acceptance-ledger surfaces but NOT the deferral test-golden family,
where the diff review found two verdicts flipped deferred(
bound_expression_unresolved) -> lowered (deferral_cnc_router
square_xy at 2e-05, deferral_dune_buggy flow at 0.62). Both flips
were verified by hand as the repair doing its job -- bounds that
needed their unit token to resolve now lower, rigor strictly
increases -- so the window stands. Recorded because the review bar
exists precisely for the changes a summary calls churn: the
quantification a regen report offers is not the review, the diff is.

## WO-G11 landed: the studio exists, and its feature audit (D260 ruling 2)

graphite 01ba8c2 (gate green, 61 Playwright specs): scan upload
(blake3-hashed into traced/scans/), calibration rungs A (similarity)
and B (DLT homography) in-house with rms/max residuals in mm and two
honesty diagnostics (photo_uncorrected_scale,
accuracy_bound_tighter_than_residual), bilinear grid capture with
per-point drag-and-confirm, pitch_basis declared never assumed
(reset-to-default REJECTED for provenance fields, D261 ruling 4),
export gated until calibration is complete. Read-only throughout;
the write seam remains WO-G12's.

The post-completion feature audit, recorded per D260 ruling 2:
- Per-point confirm TIMESTAMPS: deferred to WO-G12 (nowhere to
  persist until the .rgp writer's observation provenance exists).
- Suggested-rung hint from fit quality: ARGUED OUT -- the rungs are
  different distortion models, not quality tiers of one fit; the
  photo_uncorrected_scale diagnostic already catches the one real
  wrong-rung error.
- Undo-last-corner / reset-grid affordance: deferred; reopen if real
  tracing sessions show misclicked corners are common.
- Zoom-synced point placement: deferred to WO-G12 and recorded as a
  KNOWN GAP -- placement math reads the raw bounding rect and does
  not compose with PanZoomFrame's transform; the next WO layering
  click-to-place drawing on top must fix it first.

## F161. Environmentally-red worktree tests can mask real regressions -- WO-150's spec-contract break

WO-150's structural half changed HdlTierRow's value/margin to
DimensionedValue -- correct per D262 -- but the fleet's ship-spec JSON
contract feeds that model directly (cli/app.py _hdl_from_spec
model_validate), so riscv_hart_rv1's committed spec block with bare
floats became a ship-time refusal, and demo12's committed manifest
hashed the old shape. The agent's worktree gate could not catch it:
the exact tests that would have (wo125/126, demo12) were ALREADY red
in its worktree for an unrelated environmental reason (worktree venvs
lack the kicad/feldspar links), and the agent -- reasonably but
wrongly -- classified them as pre-existing after a git-stash rerun
showed them failing on its base too. The masking mechanism is
general: a dismissed-as-environmental test is a test that cannot
catch your regression. Coordinator integration on the fully-linked
main checkout caught it; the fix is the corpus migration D262 implies
(tier value/margin as {magnitude, unit:"1"} in spec files) plus the
demo12 regen. Two lessons recorded: (1) "confirmed pre-existing via
stash" is NOT equivalent to "cannot be affected by my change" when
the failure is environmental -- the dismissal must name the failure's
MECHANISM, not just its presence on the base; (2) worktree venvs need
the kicad/feldspar links for a meaningful fleet gate -- a dispatch
that touches ship surfaces must run make install + feldspar-link in
its worktree first (add to future dispatch prompts).
