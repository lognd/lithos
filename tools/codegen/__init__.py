"""GENERATED-artifact codegen drivers (`make schema`-style precedent).

Currently: `generate_codes` (WO-131/D247, the diagnostic code + explain
registry single-sourced from `crates/regolith-diag`).
"""
